return {
    settings = {
        Lua = {
            --[[ formatting = { ]]
            --[[     enabled = false, ]]
            --[[ }, ]]
            completion = {
                callSnippet = "Replace",
            },
        },
    },
    server_capabilities = {
        formatting = false,
    },
}
